import React from 'react'
import { Button } from './button';
import { Github } from "lucide-react";

export default function GithubAuthBtn() {
    return (
        <Button variant={'default'} onClick={() => { }}>Continue with GitHub</Button>
    );
}
